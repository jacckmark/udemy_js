/* 
DOM
-document object model,
-structured representation of an html document (can be used to
connect web pages to scripts or programing languages like js),
-on website for each html box (for example paragraph or section)
there is DOM object created that we can access and interact with,
-JS and DOM are two different things (we can manipulate DOM with 
JS),

HTML and CSS
-HTML holds content of our webpage,
-HTML body holds visual content of our webpage and head holds 
definitions of keywords, imports, is pointing to place where 
css and js is,
-CSS (presentation of the page) defines style of webpage elements 
defined in HTML,
-CSS is connected with HTML using classes, id (this way we are 
accessing elements in our page), 
*/